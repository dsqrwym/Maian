export enum CategoryType {
  PRIVATE = 'PRIVATE',
  PUBLIC = 'PUBLIC',
}

export enum CategorySelectField {
  IVA = 'iva',
  LEVEL = 'level',
  USER_ID = 'user_id',
  RELATIONS = 'relations',
  TRANSLATIONS = 'translations',
}
