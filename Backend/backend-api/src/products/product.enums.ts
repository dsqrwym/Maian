export enum ProductListSelectField {
  IVA = 'iva',
  STATUS = 'status',
  USER_ID = 'user_id',
  CATEGORY = 'category',
}

export enum ProductSelectField {
  STATUS = 'status',
  USER_ID = 'user_id',
}
